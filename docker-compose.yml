version: '3.4'

services:
  sql.data:
    image: mcr.microsoft.com/mssql/server
  studentportal.web:
    image: student-portal
    build:
      context: .
      dockerfile: StudentPortal.Web/Dockerfile


